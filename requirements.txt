# Requires Python >=3.10
# Core dependencies
torch>=2.1
transformers>=4.35.0
numpy>=1.24,<2

# Tuned lens (Experiment 2) — installed from local fork via setup instructions

# Plotting
matplotlib>=3.7.0

# Data processing
pandas>=2.0.0
tqdm>=4.65.0

# HuggingFace utilities (Experiments 2, 4)
huggingface_hub>=0.19.0
safetensors>=0.4.0
datasets>=2.14.0

# YAML config parsing (Experiments 1, 2)
PyYAML>=6.0

# Sparse eigendecomposition (Appendix B — spectral distance)
scipy>=1.10

# OLMo-native checkpoint loading (Experiments 4, 5, evaluation)
# Loaded from the local OLMo/ submodule — no pip install needed.

# Tokenization (evaluation)
tokenizers>=0.15.0

# Evaluation (lm-evaluation-harness)
lm_eval>=0.4.0
