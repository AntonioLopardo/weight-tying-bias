# Core dependencies
torch>=2.0.0
transformers>=4.35.0
numpy>=1.24.0

# Tuned lens (Experiment 2) — installed from local fork via setup instructions

# Plotting
matplotlib>=3.7.0

# Data processing
pandas>=2.0.0
tqdm>=4.65.0

# HuggingFace utilities (Experiments 2, 4)
huggingface_hub>=0.19.0
safetensors>=0.4.0
datasets>=2.14.0

# YAML config parsing (Experiments 1, 2)
PyYAML>=6.0

# Optional: for OLMo-native checkpoint loading (Experiments 4, 5)
# ai2-olmo>=0.3.0

# Optional: for faster tokenization
# tokenizers>=0.15.0
